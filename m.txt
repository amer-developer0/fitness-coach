
.hero {
  min-height:       100vh;
  padding:          8rem 7vw 6rem;
  display:          grid;
  grid-template-columns: 1fr;
  gap:              4rem;
  align-items:      center;
  position:         relative;
  overflow:         hidden;
  background:       var(--off);
}

/* Dot pattern overlay */
.hero::before {
  content:          '';
  position:         absolute;
  inset:            0;
  background-image: radial-gradient(var(--line) 1.2px, transparent 1.2px);
  background-size:  28px 28px;
  opacity:          0.7;
  pointer-events:   none;
}

/* Radial fade overlay */
.hero::after {
  content:        '';
  position:       absolute;
  inset:          0;
  background:     radial-gradient(ellipse 60% 70% at 30% 50%, rgba(247, 246, 243, 0) 0%, var(--off) 80%);
  pointer-events: none;
}

/* Content wrapper */
.hero-left {
  position:    relative;
  z-index:     1;
  animation:   heroIn 0.8s ease both;
  text-align:  center;
  align-items: center;
}

/* Tag pill */
.hero-tag {
  display:        inline-flex;
  align-items:    center;
  gap:            0.5rem;
  background:     rgba(42, 107, 74, 0.08);
  border:         1px solid rgba(42, 107, 74, 0.2);
  color:          var(--accent);
  font-size:      0.78rem;
  font-weight:    700;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  padding:        0.35rem 1rem;
  border-radius:  100px;
  margin-bottom:  2rem;
}

.hero-tag::before {
  content:       '';
  width:         6px;
  height:        6px;
  border-radius: 50%;
  background:    var(--accent);
  animation:     pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1;   transform: scale(1);   }
  50%       { opacity: 0.4; transform: scale(1.4); }
}

/* Headline */
.hero-h1 {
  font-family:   'Tajawal', sans-serif;
  font-weight:   800;
  font-size:     clamp(2.4rem, 5vw, 3.8rem);
  line-height:   1.2;
  color:         var(--ink);
  margin-bottom: 1.2rem;
}

.hero-h1 em {
  font-style: normal;
  color:      var(--accent);
  position:   relative;
}

/* Sub-copy */
.hero-sub {
  font-size:     clamp(1rem, 1.8vw, 1.15rem);
  color:         var(--mid);
  line-height:   1.85;
  max-width:     480px;
  margin-bottom: 2.5rem;
  margin-left:   auto;
  margin-right:  auto;
}

.hero-sub strong {
  color:       var(--text);
  font-weight: 700;
}

/* Action buttons */
.hero-actions {
  display:         flex;
  flex-wrap:       wrap;
  gap:             0.9rem;
  align-items:     center;
  margin-bottom:   2.8rem;
  justify-content: center;
}

.btn-main {
  display:         inline-flex;
  align-items:     center;
  gap:             0.6rem;
  background:      var(--accent);
  color:           #fff;
  font-family:     'Tajawal', sans-serif;
  font-weight:     700;
  font-size:       1rem;
  padding:         0.85rem 2rem;
  border-radius:   100px;
  text-decoration: none;
  border:          none;
  cursor:          pointer;
  box-shadow:      0 4px 20px rgba(42, 107, 74, 0.25);
  transition:      background 0.2s, transform 0.15s, box-shadow 0.2s;
}

.btn-main:hover {
  background: #1f5238;
  transform:  translateY(-2px);
  box-shadow: 0 8px 28px rgba(42, 107, 74, 0.32);
}

.btn-text {
  display:         inline-flex;
  align-items:     center;
  gap:             0.3rem;
  color:           var(--mid);
  font-size:       0.9rem;
  font-weight:     600;
  text-decoration: none;
  transition:      color 0.2s;
}

.btn-text:hover {
  color: var(--text);
}

/* Social proof row */
.proof-row {
  display:         flex;
  align-items:     center;
  gap:             1rem;
  justify-content: center;
}

.proof-avatars {
  display: flex;
}

.proof-avatars span {
  width:           34px;
  height:          34px;
  border-radius:   50%;
  border:          2px solid var(--white);
  background:      var(--line);
  display:         flex;
  align-items:     center;
  justify-content: center;
  font-size:       0.85rem;
  margin-left:     -8px;
  overflow:        hidden;
}

.proof-avatars span:nth-child(1) { background: #d4e8de; }
.proof-avatars span:nth-child(2) { background: #f5dfc0; }
.proof-avatars span:nth-child(3) { background: #dce4f0; }
.proof-avatars span:nth-child(4) { background: #f0d8e8; }

.proof-text {
  font-size:   0.82rem;
  color:       var(--mid);
  line-height: 1.4;
}

.proof-text strong {
  color: var(--text);
}